{% extends 'base.html.twig' %}

{% block title %}Configuration{% endblock %}

{% block body %}
    <h1>Configuration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ configuration.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ configuration.valeur }}</td>
            </tr>
            <tr>
                <th>Cle</th>
                <td>{{ configuration.cle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_configuration_app_index') }}">back to list</a>

    <a href="{{ path('app_configuration_app_edit', {'id': configuration.id}) }}">edit</a>

    {{ include('configuration_app/_delete_form.html.twig') }}
{% endblock %}
