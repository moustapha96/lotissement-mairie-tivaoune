<form method="post" action="{{ path('app_model_sms_app_delete', {'id': model_sm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ model_sm.id) }}">
	<button class="btn">Delete</button>
</form>
